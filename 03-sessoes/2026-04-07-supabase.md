# Sessão — 07/04/2026
**Tema:** Supabase
**Participantes:** Sócios

---

## O que foi feito

### Decisões de arquitetura
- Multi-tenant com `estabelecimento_id` em todas as tabelas
- Supabase Cloud plano Free — VPS sem RAM suficiente para self-hosted
- Tamanhos e adicionais no cardápio adiados para depois do MVP
- Campo `itens` em pedidos usando JSONB — flexível para o MVP
- Tabela `contatos` usa `UNIQUE(estabelecimento_id, telefone)` — mesmo cliente pode existir em múltiplos estabelecimentos sem conflito

### Infraestrutura Supabase
- Projeto: `agente-ia`
- Região: São Paulo (`sa-east-1`)
- URL: `https://wyngopoqjvegqpvttjxb.supabase.co`
- Status: `ACTIVE_HEALTHY`
- PostgreSQL versão 17

### Tabelas criadas e validadas

| Tabela            | RLS   | Registros       |
|-------------------|-------|-----------------|
| estabelecimentos  | ativo | 3               |
| contatos          | ativo | 4               |
| mensagens         | ativo | 16              |
| cardapio          | ativo | 7 itens de teste|
| promocoes         | ativo | 2               |
| pedidos           | ativo | sem registros   |

### Correções aplicadas
- RLS habilitado em `estabelecimentos` e `contatos` que estavam sem
- Chave estrangeira `contato_id` declarada formalmente em `mensagens`

### Acesso via MCP
- Claude tem acesso direto ao banco via MCP do Supabase
- Consegue consultar, criar e alterar tabelas sem precisar do SQL Editor
- Acesso ativo neste chat — basta reconectar em sessões futuras

---

## Problemas encontrados

- RLS estava ausente em `estabelecimentos` e `contatos` — corrigido nesta sessão
- Chave estrangeira `contato_id` em `mensagens` não estava declarada formalmente — corrigido

---

## Pendências que ficaram

Nenhuma pendência de Supabase — estrutura completa e validada.

---

## Próximos passos

1. Chat N8N — configurar credenciais do Supabase e testar as queries do fluxo
