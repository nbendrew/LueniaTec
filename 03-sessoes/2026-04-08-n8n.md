# Sessão — 08/04/2026
**Tema:** N8N — Fechamento do Fluxo Principal
**Participantes:** Sócios

---

## O que foi feito

### Code in JavaScript — payload LLM
- Monta system prompt completo com variáveis substituídas dinamicamente
- Formata cardápio: `- Nome: R$preco — descricao`
- Formata promoções: filtra apenas `ativo: true`
- Monta histórico no formato `[{role, content}]`
- Retorna body serializado via `JSON.stringify` para contornar limitação do N8N com arrays

### HTTP Request — OpenAI
- Chamada para `gpt-4o-mini` funcionando
- Body Content Type: `Raw` com `Content-Type: application/json`
- Body: `={{ $json.body }}`

### Edit Fields — extrairResposta
- Extrai `choices[0].message.content` da resposta da OpenAI
- Pendência: remover o `=` que está aparecendo no início do conteúdo salvo

### Supabase — Insert mensagem assistant
- Salvando resposta da Sofia corretamente na tabela `mensagens`
- Campos: `estabelecimento_id`, `contato_id`, `role: assistant`, `conteudo`

### HTTP Request — Evolution API
- Envia resposta ao cliente via WhatsApp
- Body Content Type: `Raw` com `JSON.stringify` para contornar quebras de linha e markdown
- URL: `http://187.127.7.78:8080/message/sendText/wa4`

---

## Fluxo completo — todos os nós

```
Webhook
  ↓
classificar e filtrar (Split Out)
  ↓
IFfromMe
  ├── false → No Operation
  └── true ↓
extracaoTelefone (TelefoneC + TelefoneE)
  ↓
coletar_id_estabelecimentos
  ↓
registrarContato (HTTP Request — upsert)
  ↓
coletar_id_contatos
  ↓
registrar_mensagens (Insert user)
  ↓
historico_mensagens (Select)
  ↓
cardapio (Select — Execute Once)
  ↓
promocoes (Select — Execute Once)
  ↓
Get a row (Select configuracoes)
  ↓
Code in JavaScript (monta payload)
  ↓
HTTP Request (OpenAI)
  ↓
extrairResposta (Edit Fields)
  ↓
Supabase Insert (assistant)
  ↓
HTTP Request (Evolution API — envia ao cliente)   ✓
```

---

## Problemas resolvidos

- Serialização do payload para OpenAI — resolvido com `JSON.stringify` + Raw body
- Quebras de linha e markdown na resposta da Sofia quebrando o JSON da Evolution API — resolvido com `JSON.stringify` direto na expressão do body

---

## Pendências que ficaram

- [ ] Remover o `=` que aparece no início do conteúdo salvo no Supabase — problema no modo do campo no nó Edit Fields
- [ ] Limpar tabela `mensagens` no Supabase — dados sujos de testes acumulados
- [ ] Testar fluxo completo de ponta a ponta com mensagem real chegando pelo WhatsApp
- [ ] Tornar instância dinâmica na URL da Evolution API (hardcoded `wa4` por ora)
- [ ] Reativar RLS com políticas corretas antes de produção

---

## Próximos passos

1. Corrigir o `=` no nó extrairResposta
2. Limpar tabela `mensagens`
3. Teste de ponta a ponta com WhatsApp real
