# Sessão — 03/04/2026
**Tema:** N8N — Fluxo Principal
**Participantes:** Sócios

---

## O que foi feito

### extracaoTelefone (Edit Fields)
- Campo renomeado: `Telefone` → `TelefoneC` (cliente)
- Campo adicionado: `TelefoneE` (estabelecimento) → `{{ $json['body.sender'].split('@')[0] }}`

### Upsert contatos (HTTP Request)
- `Continue On Fail` ativado — trata erro 409 (duplicate key) sem quebrar o fluxo
- Body funcionando:
```json
{
  "estabelecimento_id": "{{ id dinâmico }}",
  "telefone": "{{ $('extracaoTelefone').item.json['TelefoneC'] }}",
  "nome": "{{ $('IFfromMe').item.json['body.data.pushName'] }}"
}
```

### Busca estabelecimento (Supabase Get Many)
- Filtra por `telefone_whatsapp` = `TelefoneE`
- Retorna `id` do estabelecimento dinamicamente
- Problema resolvido: `\n` invisível no campo `telefone_whatsapp` causava falha no filtro

### Busca contato (Supabase Get)
- Filtra por `telefone` + `estabelecimento_id` com `Must Match: All Filters`
- Retorna `contato_id` usado no Insert de mensagens

### Insert mensagens (role: user)
- Funcionando corretamente
- Campos: `contato_id`, `estabelecimento_id`, `role`, `conteudo`

### Select mensagens (histórico)
- Funcionando corretamente
- Filtros: `contato_id` + `estabelecimento_id` com `Must Match: All Filters`
- Ordenado por `criado_em` ASC

### Select cardápio
- Funcionando corretamente
- `Execute Once` ativado — sem duplicatas
- Filtros: `estabelecimento_id` + `ativo: true`

### Select promoções
- Funcionando corretamente
- `Execute Once` ativado
- Filtros: `estabelecimento_id` + `ativo: true`

---

## Fluxo atual

```
Webhook
  ↓
Split Out
  ↓
IFfromMe
  ├── false → No Operation
  └── true ↓
Edit Fields — extracaoTelefone (TelefoneC + TelefoneE)   ✓
  ↓
Supabase — busca estabelecimento por TelefoneE            ✓
  ↓
HTTP Request — upsert contatos                            ✓
  ↓
Supabase — busca contato (retorna contato_id)             ✓
  ↓
Supabase — Insert mensagens (role: user)                  ✓
  ↓
Supabase — Select mensagens (histórico)                   ✓
  ↓
Supabase — Select cardápio (Execute Once)                 ✓
  ↓
Supabase — Select promoções (Execute Once)                ✓
  ↓
[próximos nós a construir]
```

---

## Problemas resolvidos

- Credencial Supabase com chave errada (`sb_secret_...`) — substituída pela `service_role`
- RLS desabilitado temporariamente durante debug — reativar com políticas antes da produção
- UUID do estabelecimento era hardcoded — agora buscado dinamicamente pelo `TelefoneE`
- `\n` invisível no campo `telefone_whatsapp` causava falha no filtro do Supabase
- Itens duplicados no cardápio — resolvido com `Execute Once`
- Select promoções apontava para tabela errada — corrigido

---

## Pendências que ficaram

- [ ] Definir system prompt no chat do agente antes de voltar para montar o payload

---

## Próximos passos

1. Chat LLM/Agente — definir system prompt base
2. Voltar ao N8N — montar Edit Fields com payload da LLM
3. HTTP Request — chamar OpenAI
4. Edit Fields — extrair resposta da LLM
5. Supabase — Insert mensagens (role: assistant)
6. HTTP Request — Evolution API enviar resposta ao cliente
